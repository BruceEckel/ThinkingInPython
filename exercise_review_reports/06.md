# Chapter 06: Modules and Packages — exercise pass

**Verdict:** clean
**Exercises:** 6 in the chapter, 6 in Solutions, same order, each solution
answers its own numbered exercise.
**Performed:** All six exercises run cold from the chapter alone, then
diffed against `Solutions/06_Foundations--Modules_and_Packages.md`, using
copies of `build/examples/06_Foundations--Modules_and_Packages/` in a
scratch directory, invoked as `uv run python <script>` from the repo root
(so the pinned 3.15 interpreter and project config apply).

## Findings

None. Every referent named in the six exercises exists in the chapter
under the name given (`a_package/module5.py`, `a_package/b_package/`,
`noisy.py`, `module.py`, `use_module.py`, `a_package/module4.py`,
`use_module4.py`, `exporting.py`, `star_import.py`, the `File Names`
section). Every premise held, every stated outcome reproduced exactly,
and each solution answers only what its exercise asked.

Specific reproductions:

- Ex. 1: `import a_package.module5` / `from a_package import module5` /
  `from a_package.module5 import function5` together print
  `"importing module5 in a_package"` exactly once. Matches solution.
- Ex. 2: added `a_package/b_package/module6.py`, called `function6()`
  from outside `a_package`; renamed `b_package` → `bPackage` and the
  import still worked after updating the import path to match. Also
  checked the solution's supplementary claim: renaming to all-lowercase
  `bpackage` while importing `a_package.bPackage.module6` (mismatched
  case) raises `ModuleNotFoundError: No module named 'a_package.bPackage'`
  even though the OS (Windows/NTFS) would open the directory under
  either spelling — matches the solution's explanation. Also confirmed
  the relative form `from ..module5 import function5` inside
  `module6.py` works, as the solution states.
- Ex. 3: `lazy import noisy` then `lazy import noisy2`, using `noisy2`
  before `noisy`, printed the loading messages in use order
  (`noisy2 module loaded` before `noisy module loaded`), not
  declaration order. Matches solution exactly, output-for-output.
- Ex. 4: renamed `module.py` → `Module.py`. `import Module` worked;
  changing back to `import module` (file still `Module.py`) raised
  `ModuleNotFoundError: No module named 'module'` on this Windows
  machine, confirming the case-sensitive-on-every-platform claim.
  `PYTHONCASEOK=1` made `import module` resolve to `Module.py`, as the
  solution says.
- Ex. 5: changed `a_package/module4.py` to the absolute
  `from a_package.module1 import function1`; `use_module4.py` still
  printed the same result. Running `python a_package/module4.py`
  directly failed both before (`ImportError: attempted relative import
  with no known parent package`) and after
  (`ModuleNotFoundError: No module named 'a_package'`) the change, with
  exactly the two errors the solution names. `python -m a_package.module4`
  succeeded with either import form.
- Ex. 6: removed `__all__` from a copy of `exporting.py`; the star
  import then bound `['helper', 'public', 'undeclared']`, matching the
  solution's predicted/confirmed output.

`uv run ruff check` and `uv run ty check` on
`build/solutions/06_Foundations--Modules_and_Packages` both report no
issues.

## Notes

None.
