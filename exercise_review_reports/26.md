# Chapter 26: Surrogate — exercise pass

**Verdict:** clean
**Exercises:** 7 in the chapter, 7 in Solutions
**Performed:** Ran all 7 cold from the chapter (own implementations) for
exercises 2, 4, 6, 7 (determinate outcomes), then diffed against
`Solutions/26_Patterns--Surrogate.md`; ran the Solutions' own listings
for exercises 1, 3, 5 (open-ended design tasks) to confirm their stated
outputs are real. All seven `build/solutions/26_Patterns--Surrogate/exercise_N.py`
files were executed via `uv run python`.

## Findings

None. Every referent named in an exercise exists under the name given
(`counting_proxy.py`/`CountingProxy`, `dunder_bypass.py`/`Proxy`,
`state_surrogate.py`/`Surrogate`/`change_to()`, `self._impl`,
`24_Patterns--Singleton.md`). Every premise holds. Every stated outcome
reproduced exactly:

- Exercise 2 (per-method `Counter` tally): cold implementation printed
  `2 1` (f called twice, g once), matching the exercise's stated
  result and the solution.
- Exercise 4 (misspelling `self._impl` as `self._imp`): cold
  reproduction raised `RecursionError: maximum recursion depth
  exceeded`, matching the exercise's claim and the solution's
  explanation.
- Exercise 6 (`__len__()` forwarding on `dunder_bypass.py`'s `Proxy`):
  cold implementation printed `2` for `len(p)`, matching the exercise
  and the solution.
- Exercise 7 (`change_to()` rejecting a narrower implementation): cold
  implementation raised `TypeError missing: ['g']` and left the old
  implementation in place (`s.g()` still printed `Full.g()`), matching
  the exercise and the solution.
- Exercises 1, 3, 5 (virtual proxy, copy-on-write, connection pool) are
  open-ended; ran the Solutions' listings directly and their `#:`
  markers all match live output (`uv run python
  build/solutions/26_Patterns--Surrogate/exercise_{1,3,5}.py`).

Exercise count and order match 1-for-1 between chapter and Solutions.
No renamed referents, no extra edits beyond what each exercise asked,
no numbering drift. Solutions' own explanatory claims (why
`__getattr__()` can't supply `__len__()`, why the typo recurses instead
of raising `AttributeError`, why the type checker can't police
`change_to()`) all match the chapter's own explanation of the same
mechanisms and the observed runtime behavior.

## Notes

None.
