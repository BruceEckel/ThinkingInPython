# Chapter 21: Design Patterns — exercise pass

**Verdict:** clean
**Exercises:** 3 in the chapter, 3 in Solutions
**Performed:** All three exercises are open-ended (name a vector of
change in your own program; subtract a pattern you know; apply
Subtraction to a design of your own), so there is no single
determinate answer to reproduce cold. I read the exercise text and the
chapter body it depends on, then verified every solution's code and
every substantive claim by running it: `exercise_1a.py`, `exercise_1b.py`,
`exercise_2.py`, `exercise_3.py`, and the chapter's one listing
`strategy_is_a_function.py`, all via `uv run python`. I additionally
wrote two scratch probes (not committed) to check the solution's `ty`
claims: that `checkout()` in exercise 2 rejects a wrongly-shaped
function argument, and that `checkout()` in exercise 3 rejects an
object that doesn't subclass `Shipping`. Both checked with `uv run ty
check`.

## Findings

None. No blocking, wrong, drift, or minor findings.

## Notes

- The chapter's exercises name no specific chapter listing, function,
  class, or file to edit (unlike, e.g., a "modify weak_value.py"
  exercise); they ask the reader to work from a program of their own.
  So point 1 (referents exist) and point 3 (stated outcome, for a
  concrete listing) mostly do not apply here — there is nothing chapter-side
  to mis-cite. I checked what *is* checkable: the solution's own code
  runs and prints what its `#:` markers say, and the solution's factual
  claims about the code hold.
- All four solution listings' `#:` markers matched real output exactly:
  `exercise_1a.py` → `pens,3` / `paper,7`; `exercise_1b.py` →
  the two JSON lines; `exercise_2.py` and `exercise_3.py` → `25.0 23.0`
  each.
- Verified by direct test: exercise 2's claim "the type checker still
  knows what the parameter accepts... `Callable[[float], float]`
  rejects a function taking the wrong arguments" — confirmed, `ty`
  raises `invalid-argument-type` when passing a `def wrong(a: str) ->
  float` to `checkout()`.
- Verified by direct test: exercise 3's parallel claim ("`ty` still
  rejects a wrongly-shaped argument") for the class-based version —
  confirmed, `ty` raises `invalid-argument-type` when passing a
  non-`Shipping` object with a matching `cost()` method to
  `checkout()`.
- Verified by direct test: exercise 3's claim that removing
  `checkout()`'s `shipping` parameter (inlining `5.0`) still runs and
  prints a number — confirmed, prints `25.0`.
- Exercise count and order match: chapter exercises 1-3 correspond to
  Solutions headings "1. Naming a vector of change", "2. Subtracting a
  pattern", "3. Applying *Subtraction*", in the same order, each
  answering its own number.
- Solution 3 goes slightly past what exercise 3 asks (it removes the
  abstract base first, confirms nothing broke, then removes one more
  thing — the `shipping` parameter — to find something that does
  break). Exercise 3 only requires reporting what stopped working, or
  leaving it out if nothing did. This is extra exploration, not a
  contradiction of the exercise or a silent substitution of a
  different task, so I am not flagging it as a defect — noting it here
  in case a human reader weighs it differently.
- Solution 2's link to `[When a Pattern
  Dissolves](../Chapters/21_Patterns--Design_Patterns.md#when-a-pattern-dissolves)`
  resolves: the chapter has a `## When a Pattern Dissolves` heading at
  that pandoc-slugged anchor.
- `uv run ty check` and `uv run ruff check` both pass clean on
  `build/solutions/21_Patterns--Design_Patterns` and
  `build/examples/21_Patterns--Design_Patterns`.
