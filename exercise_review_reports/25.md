# Chapter 25: Template Method — exercise pass

**Verdict:** clean
**Exercises:** 4 in the chapter, 4 in Solutions
**Performed:** all four run cold, then diffed against Solutions.

- Exercise 1 (file-processing framework): wrote and ran the
  subclassing and function-passing forms for the uppercase
  customization from the exercise's own description (own code,
  matches `Solutions/exercise_1.py`'s `demo()` output
  `'HELLO\nWORLD\n'` twice). Also implemented the word-search
  customization (part ii), which Solutions only describes in prose,
  to confirm the design claim "needs no change to `FileFramework.run()`"
  holds: a `WordSearchFramework(FileFramework)` that reads the word
  list in `__init__()` and overrides only `process()` works with no
  base-class change. Ran it in scratch; correct output.
- Exercise 2 (repair `premature_engine.py`): wrote both the "reorder"
  and "redesign" repairs myself before reading Solutions. Both ran
  and printed `Hello, Robin!`. Matches `Solutions/exercise_2_reorder.py`
  and `exercise_2_redesign.py` (they use "Brian" instead of "Robin";
  cosmetic only).
- Exercise 3 (reversed `run()`): wrote a `Reversed(ApplicationFramework)`
  subclass overriding `run()`. Ran it (Python executes, prints the
  reversed order); ran `ty check` on it (rejects the override with
  `error[override-of-final-method]`, citing `@final`). Matches
  `Solutions/exercise_3.py` and its stated `ty` error text.
- Exercise 4 (`Exploder`/`HalfDone`): wrote both subclasses. Ran them
  (`Exploder` raises `RuntimeError: step 1 refuses`; `HalfDone.pending`
  grows to `['work', 'work']`); `ty check` reports nothing for either.
  Matches `Solutions/exercise_4.py` exactly.

All referenced files/names (`premature_engine.py`, `Greeter.__init__()`,
`ApplicationFramework`, `run()`, `customize1()`, `customize2()`) exist in
the chapter, spelled as named. Exercise count (4) and Solutions count (4)
match, in the same order, each solving its own number. Spot-checked one
of Solutions' own cross-chapter claims — the link to
`24_Patterns--Singleton.md#when-you-want-a-class-cache-the-instance` — and
the anchor exists.

## Findings

None reproduced.

## Notes

No suspected-but-unreproduced issues. Solutions exercise 1 only codes
the uppercase customization in both forms and leaves the word-search
customization as a prose design description rather than runnable code;
I implemented it myself to confirm the design claim is true (it is), so
this is not reported as a defect, just recorded here since it's the one
place the solution is less than fully executable as given.
