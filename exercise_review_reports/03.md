# Chapter 03: Containers — exercise pass

**Verdict:** clean
**Exercises:** 9 in the chapter, 9 in Solutions
**Performed:** ran all nine cold (my own code, from the chapter body and
exercise text alone), then diffed against `Solutions/03_Foundations--Containers.md`.
No exercise was "read only" — all nine involve a determinate, checkable
result and all nine were executed.

## Findings

None. No defect reproduced.

## Notes

Verification detail, chapter alone -> my run -> Solutions comparison:

- **Ex 1** (`deque_timing.py`, n: 20_000 -> 2_000 -> 200_000). Ran both
  sizes with `uv run python`. At `n=2_000`: `list=0.000617
  deque=0.000078`, `deque_time < list_time` is `True`. At `n=200_000`:
  `list=8.016517 deque=0.010719`, still `True`, and the list variant did
  take several seconds, matching the chapter's claim. The chapter's own
  listing prints `deque_time * 20 < list_time` (a stronger comparison
  than the exercise asks about), and the exercise/solution both use the
  plain `deque_time < list_time`; both are true at all three sizes so
  this is not a discrepancy, just two different comparisons that both
  hold.
- **Ex 2** (`defaultdict.py`, count by kind). Reproduced
  `{'dog': 2, 'cat': 1}`, matches Solutions exercise 2.
- **Ex 3** (`set_methods.py`, three-set union/intersection). Reproduced
  `{1, 2, 3, 4, 5, 9}` and `set()`, matches Solutions exercise 3.
- **Ex 4** (`immutable_containers.py`, `groups.add([1, 2])`). Reproduced
  `TypeError: cannot use 'list' as a set element (unhashable type:
  'list')`, matches Solutions exercise 4. Hashability, needed to answer
  the "explain" half, is covered in the chapter body (lines 270-271,
  665-778 of `Chapters/03_Foundations--Containers.md`), so the exercise
  is answerable from the chapter alone.
- **Ex 5** (slices of `xs = [10, 20, 30, 40, 50]`). Reproduced
  `[40, 50]`, `[20, 30, 40]`, and `[40, 30, 20]` (both the
  slice-then-reverse and the one-slice `xs[3:0:-1]` forms), matches
  Solutions exercise 5. "The middle three" of a 5-element list is
  unambiguous (indices 1-3).
- **Ex 6** (`counter.py` tally rewritten with `defaultdict(int)`).
  Reproduced `{'the': 3, 'cat': 2, 'sat': 1, 'on': 1, 'mat': 1}`, matches
  Solutions exercise 6.
- **Ex 7** (`heterogeneous.py` as a `namedtuple`). Built the
  `namedtuple`, unpacked it with the same `name, age, height = person`
  line from `heterogeneous.py`, reproduced `Alice 30 1.65`; the
  unpacking line is verified unchanged, matches Solutions exercise 7.
- **Ex 8** (`dict` from `pairs`, merge with `{"c": 30, "d": 4}`).
  Reproduced keys `['a', 'b', 'c']`, values `[1, 2, 3]`, and merge
  `{'a': 1, 'b': 2, 'c': 30, 'd': 4}` — `"c"` ends up `30`, matches
  Solutions exercise 8's explanation (right operand wins).
- **Ex 9** (unpacking `row = [1, 2, 3, 4, 5]` without indexing).
  Reproduced `first, middle, last` = `1 [2, 3, 4] 5`; `a, b = row`
  raised `ValueError: too many values to unpack (expected 2, got 5)`;
  `a, *b = row` succeeded as `1 [2, 3, 4, 5]`. Matches Solutions
  exercise 9.

Referent check: every named listing (`deque_timing.py`, `defaultdict.py`,
`set_methods.py`, `immutable_containers.py`, `counter.py`,
`heterogeneous.py`) exists in `build/examples/03_Foundations--Containers/`
under exactly that name, and every function/variable/method the
exercises name (`defaultdict(list)`, `groups`, `xs`, `pairs`, `row`,
etc.) exists in the chapter body spelled the way the exercises spell it.

Numbering: 9 exercises at `Chapters/03_Foundations--Containers.md:793-822`,
9 solutions in `Solutions/03_Foundations--Containers.md` (`## 1` through
`## 9`), same order, each solving its own number, no extra or missing
edits beyond what each exercise asked.
