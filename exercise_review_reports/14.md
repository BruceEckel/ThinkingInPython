# Chapter 14: Decorators — exercise pass

**Verdict:** clean
**Exercises:** 6 in the chapter, 6 in Solutions (numbered 1-6, in order,
each solving its own number)
**Performed:** all six exercises, cold, from the chapter alone, before
reading Solutions: `announce` (1), `timing` stacked with `@trace` (2),
coffee-shop Decorator pattern (3), class-based `trace` with a shared
counter (4), `memo` with/without parentheses (5), `retry(times)` (6).
Then diffed each against `Solutions/14_Techniques--Decorators.md` and
ran every solution listing directly.

## Findings

None. No blocking, wrong, drift, or minor findings reproduced.

## Notes

Checks performed, all confirmed by running code with `uv run python`
from a scratch copy of `build/examples/14_Techniques--Decorators/`:

- **Referents exist and are spelled correctly.** `register` (`register.py`,
  line 730-748), `@trace` (`tracer.py`, line 132), `count_calls.py`
  (line 475), `pizza_decorator.py`'s Decorator pattern (line 885) all
  exist in the chapter as named.
- **Premises hold.** `register()` really does return `cls` unchanged
  (matches exercise 1's comparison point). `count_calls.py`'s `count` is
  genuinely per-instance (`self.count = 0` in `__init__`), matching
  exercise 4's comparison claim about "the per-instance `count`."
- **Stated outcomes reproduced by running, not reasoning:**
  - Ex 1: `decorating Point` / `decorating Empty` / `Point Empty`,
    both class names unchanged.
  - Ex 2: with `@trace` (chapter's own `tracer.py`) wrapping my own
    `timing`, output order is `-> add(2, 3)`, then the timing line,
    then `<- add = 5` — outer decorator's prints bracket the inner
    one's, as predicted.
  - Ex 3: `Whipped(ExtraShot(Espresso()))` → `Espresso + ExtraShot +
    Whipped: $4.15`.
  - Ex 4: `f(1); f(2); g(3)` → `f.count, g.count, trace.total_calls`
    = `2 1 3`; class-level counter accumulates across both decorated
    functions while each instance's own count stays separate.
  - Ex 5: bare `@memo` on `square` computes once and caches; `@memo(maxsize=2)`
    on `add` evicts the oldest entry (`add(1, 2)`) once a third key
    arrives, confirmed by re-adding it and seeing it recompute.
  - Ex 6: `retry(times=3)` on a function that fails twice then succeeds
    prints two "attempt N failed" lines then returns the result, and
    `flaky.__name__` survives via `@wraps`.
  All six of my independently-written versions matched the `#:` markers
  in `Solutions/14_Techniques--Decorators.md`, and running the actual
  extracted solution files (`build/solutions/14_Techniques--Decorators/`)
  reproduced every marker exactly.
- **Doable from the chapter alone.** Exercise 5's "checking whether the
  decorator's first argument is callable" technique (the double-duty
  `@memo` / `@memo(maxsize=...)` decorator) isn't demonstrated
  elsewhere in the chapter, but the exercise text supplies the needed
  fact inline, so a reader isn't missing anything. `@overload`, which
  the solution uses for the two call shapes, is introduced earlier, in
  chapter 8 (Static Types).
- **Solution answers the exercise as stated**, no more and no less, for
  all six; no renaming of exercise-named entities, no silently-added
  extra edits, no skipped sub-questions.
- Minor, not reported as a finding: the chapter's own `decoration_time.py`
  (line 80) already defines a function-based decorator named `announce`;
  exercise 1 asks for a *class* decorator also named `announce`. The two
  never collide (different files, different exercise), and nothing in
  the exercise or solution is inconsistent because of it, so this isn't
  a defect, just a name reused in a different scope.
