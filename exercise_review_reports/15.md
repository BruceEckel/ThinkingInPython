# Chapter 15: Context Managers — exercise pass

**Verdict:** clean
**Exercises:** 7 in the chapter (`Chapters/15_Techniques--Context_Managers.md:848-867`), 7 in Solutions, same order, same numbers.
**Performed:** All 7 exercises run cold from the chapter, then diffed against `Solutions/15_Techniques--Context_Managers.md`.

## Findings

None. Every exercise's named referents (`trace_cm.py`, `demo_exceptions.py`,
`multiple.py`, `test_object_pool.py`, `context_decorator.py`, `exit_stack.py`,
and the classes/functions within them: `Trace`, `ignore`, `tag`, `Pool.lease`,
`pool.available()`, `banner`) exist in the chapter exactly as spelled. Every
premise held (e.g. `ignore(ZeroDivisionError)` is present in `demo_exceptions.py`
to be changed; `multiple.py`'s `with` line has exactly two managers to which a
third can be added). Every stated outcome reproduced exactly:

- Ex. 1 (nest `Trace("B")` inside `Trace("A")`): produced
  `enter A / inside A / enter B / inside B / exit B / exit A` — matches
  Solutions #1 verbatim.
- Ex. 2 (`ignore((ZeroDivisionError, TypeError))`, raise `TypeError`):
  produced `before / TypeError('nope') / survived` — suppression confirmed,
  matches Solutions #2's behavior (verified against both the actual
  `utils/exceptions.ignore` the chapter imports, and the solution's
  simplified reimplementation; both suppress correctly).
- Ex. 3 (third `tag("li")` on `multiple.py`'s `with` line): produced
  `<ul> / <li> / <li> /   ul then li then li / </li> / </li> / </ul>` —
  exit order reverses entry order, matches Solutions #3 verbatim.
- Ex. 4 (nested `pool.lease()` in `test_object_pool.py`): added a test
  leasing both connections; `pool.available()` reached `0` while both
  leased and `2` after both returned. `uv run pytest` passed 4/4
  (3 original + 1 new). Matches Solutions #4.
- Ex. 5 (stack `@banner("outer")`/`@banner("inner")`): produced
  `=== outer === / === inner === / quarterly numbers / === inner ends === /
  === outer ends ===`, and reran `report()` a second time to confirm the
  solution's claim that each call recreates the generator (same four lines
  printed again, not exhausted). Matches Solutions #5.
- Ex. 6 (`ignore_missing` suppressing only `KeyError`): `KeyError` block
  printed `survived the KeyError`; `ValueError` block propagated and was
  caught outside as `escaped: not a lookup problem`. Matches Solutions #6.
  Confirmed `contextlib.suppress` is not used.
- Ex. 7 (`exit_stack.py` driven by `sys.argv[1:]`): ran with no arguments
  (`using []`, nothing opened or closed) and with three names x/y/z
  (`open x/y/z`, `using [...]`, `close z/y/x`). Both match Solutions #7
  exactly, including the empty-argument case.

Exercise and solution counts match (7/7), in the same order, each solving
its own number. No renumbering, no extra/missing edits, no wrong dimension
or count found.

## Notes

None.
