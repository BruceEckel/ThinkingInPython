# Chapter 33: Visitor — exercise pass

**Verdict:** clean
**Exercises:** 3 in the chapter, 3 in Solutions
**Performed:** All three exercises performed cold (own code written and run)
before opening `Solutions/33_Patterns--Visitor.md`, then diffed against the
solutions. Solution listings (`build/solutions/33_Patterns--Visitor/exercise_{1,2,3}.py`)
were also run, type-checked (`ty`), and linted (`ruff`) directly from the
repo's generated tree.

## Findings

None.

## Notes

Verification detail, for the record:

- **Exercise 1** ("Rewrite `flower_visitors.py` with `singledispatch`... which
  classes and methods disappear?"). Wrote an independent singledispatch
  version of `pollinate()`/`eat()` with `Chrysanthemum`'s toxicity as a
  registered `eat()` implementation; ran it with the same `random.seed(47)` /
  `flower_gen(4)` loop from the chapter and got output identical to
  `flower_visitors.py`'s `#:` markers. `ty check` passes on my version. The
  solution's answer ("`Visitor`, `Bug`, `Pollinator`, `Predator`, `Bee`,
  `Fly`, `Worm`, and the two `visit()` methods disappear, along with
  `accept()`") matches what actually vanishes when you make this change.
  `build/solutions/.../exercise_1.py` runs and matches its own `#:` markers;
  `ty` and `ruff` are clean.
- **Exercise 2** (add `Rose`, add `thorns()`, compare line cost). Ran
  `build/solutions/.../exercise_2.py`: output matches its `#:` markers
  (`Rose: abundant nectar / strong / sharp` and `none` for `Gladiolus`'s
  `thorns()`). Checked the line-count claims against the actual listing:
  `Rose` costs 2 lines (class + `pass`) plus a 3-line registration each for
  `nectar` and `fragrance` (8 total); `thorns()` costs a 3-line base function
  plus a 3-line `Rose` registration (6 total). 6 < 8, consistent with the
  solution's conclusion that adding an operation is cheaper here.  "four
  flowers" in the exercise refers to the post-`Rose` count
  (`Gladiolus`/`Ranunculus`/`Chrysanthemum`/`Rose`), which is correct. `ty`
  and `ruff` are clean on the solution file.
- **Exercise 3** (`Visits` protocol in place of `Any`; add `Beetle(Bug)` with
  no `visit()`; which version reports the mistake, and when?). Built both
  versions myself: with `visitor: Any`, `ty check` reports no error and
  `Gladiolus().accept(Beetle())` fails at runtime with
  `AttributeError: 'Beetle' object has no attribute 'visit'`; with
  `visitor: Visits` (the `Protocol` from the chapter's "Price of the Empty
  Base" section), `ty check` on the same call reports
  `error[invalid-argument-type]: ... Expected 'Visits', found 'Beetle' ...
  protocol member 'visit' is not defined on type 'Beetle'` before the
  program runs at all. This confirms the solution's claim exactly: `Any`
  defers the mistake to runtime, `Visits` catches it statically. The
  solution's own listing keeps the call reachable at runtime via a
  `# type: ignore` on that one line; removing the ignore reproduces the same
  `ty` diagnostic (checked). `ty` and `ruff` are clean on
  `build/solutions/.../exercise_3.py`.

Exercise/solution numbering and count line up 1:1 in order. All three
solutions answer only what their exercise asked (no unrequested extra edits,
no renamed identifiers beyond what the exercise itself required). No
referent (file, class, method, or listing name) in any exercise was found
missing or misspelled.
