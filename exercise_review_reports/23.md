# Chapter 23: Iterators — exercise pass

**Verdict:** clean
**Exercises:** 10 in the chapter, 10 in Solutions
**Performed:** ran all 10 solutions (`exercise_1.py`..`exercise_10.py`,
plus `test_ch23_filter.py` for exercise 6) via `uv run python`/`uv run pytest`
from `build/solutions/23_Patterns--Iterators/` and compared output against
the `#:` markers and the exercise wording. Also independently checked, from
the chapter body alone (cold, before opening `Solutions/`), the premise of
every exercise against the named listing:
`iterators.py` (ex. 1, 3), `Countdown`/generators (ex. 2), `generator_lifecycle.py`
(ex. 4), `tee.py` and its closing rule (ex. 5), `test_endless.py` and the
`if`-clause/`filter()` pairing in "Reusable Algorithms" (ex. 6),
`gof_iterator.py` (ex. 7), the iterator protocol (ex. 8), `yield_from.py`'s
`flatten()`/`flatten_loop()` (ex. 9), `typed_generator.py`/`typed_iterator.py`
(ex. 10).

## Findings

None.

## Notes

Everything checked out:

- **Referents.** Every named file, function, class exists verbatim in the
  chapter: `iterators.py` (`total`, `fibonacci`, `Countdown`),
  `generator_lifecycle.py`, `tee.py`, `test_endless.py`, `gof_iterator.py`
  (only `OverStream`, no `OverSequence` — matches exercise 7's premise
  that the chapter "shows only the stream version"), `flatten()`/
  `flatten_loop()` in `yield_from.py`, `typed()` in `typed_generator.py`,
  `TypedIterator` in `typed_iterator.py`.
- **Premises hold.** `generator_lifecycle.py`'s second `list(sq)` really is
  `[]` (ex. 4). The chapter really pairs the `if` clause with `filter()`
  ("its lookalike is the `if` clause of a generator expression (or
  `filter()`)", line 452-454) and `test_endless.py` really has no test
  exercising `filter()` (ex. 6). `flatten()` really has no `str` guard
  (ex. 9). `typed()` really raises and stops the stream on the first
  mismatch (ex. 10).
- **Stated outcomes reproduced.** Ran every solution file; every printed
  value matched its `#:` marker exactly, including the ones with an implied
  answer: ex. 5's `tee` lockstep buffer stayed under 1% of a drained
  branch (measured 2,592 bytes vs. 4,095,200 with `--numbers`), ex. 7's
  `len(stream.seen)` was exactly `50000` after 50,000 steps on an endless
  source, ex. 9's `flatten(mixed)` raised `RecursionError` as stated.
- **Solutions answer what was asked, no more/no less.** Exercise 4 asks for
  exactly two fixes; the solution gives exactly two. Exercise 9 asks to
  explain the `RecursionError`, fix `flatten()`, and say what the same fix
  looks like in `flatten_loop()`; the solution does all three (the
  `flatten_loop()` part is answered in prose, matching the exercise's "say
  what it would look like" rather than "write it"). Exercise 6's solution
  file is `test_ch23_filter.py` (not `exercise_6.py`, since the exercise
  asks to add a test to `test_endless.py` rather than write a numbered
  exercise script) and its prose explicitly names
  `test_the_if_clause_skips_but_never_stops()` as the sibling test, which
  the exercise asked for ("say which existing test it should resemble").
- **Count/numbering match.** 10 exercises, 10 solution sections numbered
  1-10 in the same order, each solving its own number.
- **Solution's own claims hold.** Ran every solution script; all printed
  output matches both the `#:` markers and the prose claims made about it
  (e.g. ex. 2's `len(c)` unchanged after iterating, ex. 8's `peek()`
  returning `2 2 2` three times without advancing, ex. 10's `typed_skipping`
  and `SkippingIterator` both yielding `[1, 3, 4]`).

No blocking, wrong, drift, or minor findings to report for this chapter.
