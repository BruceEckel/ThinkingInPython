# Chapter 11: Testing — exercise pass

**Verdict:** clean
**Exercises:** 5 in the chapter (Chapters/11_Techniques--Testing.md:747-763), 5 in Solutions (Solutions/11_Techniques--Testing.md, headers at lines 3, 65, 107, 159, 211), same order, each solving its own number.
**Performed:** All 5 exercises performed cold (written and run from the chapter's own `account.py`/`weather.py`, before reading Solutions), including the two exercises with a determinate stated outcome (4 and 5), where I actually renamed the identifier named in the exercise and re-ran the tests to confirm which one broke.

## Findings

None. No defects reproduced.

## Notes

Cross-checks performed, all confirmed by execution (`uv run pytest ...` from repo root, copies of the chapter's `build/examples/11_Techniques--Testing/` files in scratch):

- **Ex 1** (`transfer()`): added `transfer(other: Account, amount: float)` to the chapter's actual `account.py` (a `@dataclass`), wrote the success + overdraft tests first, both passed. Matches Solutions #1's method body and test shapes exactly (`withdraw()` before `deposit()`, so an overdraft never touches either balance).
- **Ex 2** (`parametrize` + `add_interest`): parametrized rates including `0.1`, all passed with `pytest.approx()`. Verified the solution's own claim ("For 0.0, 0.05, 0.5, and 1.0 ... bit-identical ... 0.1 ... differ by one bit") by direct float comparison: `0.1` gives `110.0 != 110.00000000000001` (`==` is `False`), the other four rates are bit-identical. Claim holds.
- **Ex 3** (invariant fixture): fixture yields an `Account`, asserts `balance >= 0` after `yield`, used in two tests (full withdrawal to zero, and a deposit). Both pass. Matches Solutions #3.
- **Ex 4** (`settings_path()`): wrote both the `os.environ["APP_CONFIG"]` version and the directory-argument version, tests passed. Then actually renamed `APP_CONFIG` to `APP_SETTINGS_DIR` in a copy and reran: the environment-reading test failed with `KeyError: 'APP_SETTINGS_DIR'` as Solutions #4 states, the argument-based test still passed. Matches Solutions #4's stated ordering and stated exception exactly.
- **Ex 5** (`weather.current_temp()`/`urlopen()`): confirmed `weather.py` in the chapter's own listing calls `urlopen()` inside `current_temp()`. Wrote a second `current_temp_with(city, fetcher)` function, tested both a `monkeypatch.setattr(weather, "urlopen", ...)` version and a plain-function-injection version, both passed. Then actually renamed `urlopen` to `fetch` (via `from urllib.request import urlopen as fetch`, call site updated) in a copy and reran: the patched test failed with `AttributeError: <module ...> has no attribute 'urlopen'`, exactly the message Solutions #5 quotes; the injected test still passed. Matches Solutions #5.
- Ran the full Solutions/11 test suite (all 5 files, 12 tests total) together: all 12 pass with no collection or fixture-name collisions.
- All referents named in the exercises (`Account`, `add_interest()`, `transfer()` target, `weather.current_temp()`, `urlopen()`) exist in the chapter body under those exact names.
