# Chapter 02: Tour — exercise pass

**Verdict:** clean
**Exercises:** 6 in the chapter, 6 in Solutions
**Performed:** all six exercises performed cold (own implementation, run
with `uv run python`) before opening `Solutions/02_Foundations--Tour.md`,
then diffed against the solution's code and output. Also ran every
solution script directly, and ran `ruff`/`ty` against
`build/solutions/02_Foundations--Tour/`.

## Findings

None.

## Notes

Detail on what was checked, since the chapter came back clean:

1. **Referents exist.** All six named listings (`references.py`,
   `truthiness.py`, `fstrings.py`, `arithmetic.py`, `bitwise.py`,
   `tstrings.py`) exist in the chapter body with those exact names, at
   `Chapters/02_Foundations--Tour.md:94,157,215,269,391,437`.

2. **Premises hold.**
   - Ex 1: `c = a[:]` is present in `references.py`, ready for the
     append line.
   - Ex 4: `arithmetic.py` defines `total` exactly once (`total = 0`,
     `total += 5`), and `bitwise.py` defines `flags` exactly once
     (`flags = 0`, two `|=`). This is the exact defect class the brief
     names for this chapter ("Chapter 02's exercise 4 miscounted the
     variables in `arithmetic.py`") — checked specifically and found
     correct in the current text: one variable in each file, matching
     the solution's single rename of each.

3. **Stated outcomes reproduced, by running the code:**
   - Ex 1: `[1, 2, 3, 4] [1, 2, 3, 4, 99]` — only `c` changed.
   - Ex 2: `bool()` over `[0, 1, "", "hi", [], [1], None, {}, {"k": 1}]`
     matches the solution's marker line for line.
   - Ex 3: `Alice scored 91.50` / `score = 91.5`.
   - Ex 4: renamed to `totalSum`/`flagBits` and to `TOTAL_SUM`/
     `FLAG_BITS`; both variants ran and printed `5 0b1010`. Checked the
     linter claim directly: `uv run ruff check --select N` gave `All
     checks passed!` on the `snake_case` and `UPPER_CASE` variants and
     five `N816` ("should not be mixedCase") warnings on the camelCase
     variant — confirms "the camelCase form is the only one a linter
     objects to."
   - Ex 5: `quoted(message)` prints `'Alice' scored '92'%`, matching the
     solution. Also reproduced the prose's `name = "scored"` collision
     example: `f"{name} scored {score:.0f}%"` prints
     `scored scored 92%`, confirming the stated ambiguity.
   - Ex 6: `print(-9 // 4, -9 % 4)` prints `-3 3`, matching the
     solution and the stated identity
     `a == (a // b) * b + a % b` (`-9 == -3*4 + 3`).

4. **Doable from the chapter alone.** Each exercise draws only on
   material already presented earlier in the same chapter (slicing/
   aliasing, truthiness/`bool()`, f-string format specs, the Naming
   Conventions section, `Template`/`Interpolation` iteration, floor
   division). No outside fact required.

5. **Solutions answer what was asked, no more.** Each solution edits
   only the file(s) the exercise names, with no unrelated renames or
   extra edits.

6. **Count/order match.** 6 exercises, 6 solutions
   (`exercise_1`..`exercise_6`, plus `exercise_4_constants.py` as a
   second file for exercise 4's second rename, which the exercise text
   itself asks for), in the same order, each answering its own number.

7. **Solution's own claims hold.** Spot-checked and reproduced: the
   linker claim in exercise 4 (above), and the `ty`/`ruff` cleanliness
   of `build/solutions/02_Foundations--Tour/` (`All checks passed!` for
   both).

Both cross-reference links used by exercise 4 and its solution resolve:
`Chapters/02_Foundations--Tour.md:513` links to `#naming-conventions`,
which exists at `Chapters/02_Foundations--Tour.md:477`;
`Solutions/02_Foundations--Tour.md:92` links to
`../Chapters/02_Foundations--Tour.md#naming-conventions`, the correct
relative path from `Solutions/` to `Chapters/`.
