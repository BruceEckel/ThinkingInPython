# Chapter 24: Singleton — exercise pass

**Verdict:** clean
**Exercises:** 6 in the chapter, 6 in Solutions
**Performed:** All six exercises were done cold from the chapter, then
diffed against `Solutions/24_Patterns--Singleton.md`.

- Exercise 1 (eager initialization of `singleton_pattern.py`): written and
  run.
- Exercise 2 (connection-pool factory): open-ended; wrote a pool similar
  in shape to the solution's, confirmed the premise (`singleton_cached_factory.py`
  exists and is a reasonable starting point) and ran the solution's own
  listing.
- Exercise 3 (rewrite as a module): wrote `only_one.py`/`use_only_one.py`
  from scratch and ran it.
- Exercise 4 (`shared_config.py` rebinding): wrote and ran it, predicted
  both printed values before running.
- Exercise 5 (lock in the wrong place in `singleton_cached_race.py`):
  written and run 3 times; also timed the lock vs. no-lock variants to
  check the solution's quantitative claim.
- Exercise 6 (second `Borg` subclass): written and run.

## Findings

None. No blocking, wrong, or drift-class defects found.

## Notes

- All six listings the exercises name (`singleton_pattern.py`,
  `singleton_cached_factory.py`, `shared_config.py`,
  `singleton_cached_race.py`, `singleton_borg.py`) exist in the chapter
  under those exact names, and both section links the exercises use
  (`#tests-threads-and-locks`, `#a-module-is-already-a-singleton`) resolve
  to real headings.
- Exercise 1: eager-initializing `OnlyOne.instance` in the class body and
  dropping the `if OnlyOne.instance is None` guard reproduces the
  chapter's own claim exactly. Ran my version:
  `uv run python exercise_1_mine.py` → `['sausage', 'eggs', 'spam']` /
  `False True`, matching `singleton_pattern.py`'s original output and the
  solution's `['sausage', 'eggs'] False True`.
- Exercise 4: predicted `{'theme': 'dark'}` then `{}` before running,
  based on the mutate-vs-rebind distinction the chapter states. Ran it
  (`uv run python exercise_4_mine.py`) and got exactly that. The
  solution's `exercise_4.py` places `import config` at the top of the
  file rather than "at the end" as the exercise's wording literally
  requests; I confirmed this makes no behavioral difference (module
  caching makes import position irrelevant here — reran with `import
  config` literally at the end and got the identical output), so this is
  a wording/ordering nicety, not a defect, and I'm not reporting it as a
  finding.
- Exercise 5: added a `threading.Lock` inside `settings()`'s body only
  (matching "wrapping only the body of the cached function"), ran it
  three times, got `8` distinct objects every time — the premise that
  "the object count does not drop to one" holds. Timed the lock vs.
  no-lock versions: no lock ≈ 0.052 s, with lock ≈ 0.404 s, matching the
  solution's "about 50 milliseconds" / "about 400" claim closely.
- Exercise 6: constructed one `Singleton` and one new subclass, both via
  `Borg`; `Singleton.val` came back as the second subclass's value
  (`"eggs"`), confirming "explain the value you get back" has a real,
  reproducible answer, matching the solution's `exercise_6.py` output
  exactly (`eggs eggs True` / then `spam beans False` after giving the
  new subclass its own `_shared_state`).
- All 6 `build/solutions/24_Patterns--Singleton/exercise_N.py` files ran
  and their stdout matched every `#:` marker in
  `Solutions/24_Patterns--Singleton.md`.
