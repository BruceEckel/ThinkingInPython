# Chapter 04: Control Flow — exercise pass

**Verdict:** clean
**Exercises:** 9 in the chapter, 9 in Solutions
**Performed:** all 9 run cold from the chapter, then diffed against Solutions

## Findings

None.

## Notes

Cold-performed each exercise before opening Solutions, from a copy of
`build/examples/04_Foundations--Control_Flow/` in scratch space, always via
`uv run python`.

1. `find_factor(97)`: 97 is prime, loop never breaks, `else` clause runs.
   Matches Solutions exercise_1 exactly.
2. Added `odd_count` to `collatz_sequence(10)`: 6 steps, 1 odd step
   (`5 -> 16` is the only odd start). Matches Solutions exercise_2's
   `(6, 1)`.
3. Swapped `break`/`continue` order in `break_continue.py`: output
   unchanged (`0 1 2 4 5`), because `n == 3` and `n == 6` are mutually
   exclusive per iteration. Matches Solutions exercise_3's explanation.
4. `divide_and_report(1, "x")`: raises `TypeError` from `1 / "x"` inside
   `checked_divide()`, not caught by `except ValueError`, `finally` still
   runs first. Solutions exercise_4 wraps the call in an outer
   `try/except TypeError` and prints `type(e).__name__` rather than
   letting a real traceback hit stderr — this is the book's established
   convention for deterministic `#:`-marker output (see project CLAUDE.md
   "print `type(e).__name__` instead of a message"), not a drift from the
   exercise's intent.
5. Added `case ["go", direction, distance]` to `pattern_matching.py`:
   before, `run("go north 3")` returns `"unknown command"` (3-item list
   fails the 2-item pattern); after, it matches the new case. Verified
   the new case's position relative to `["go", direction]` doesn't matter
   (patterns are mutually exclusive by list length), matching Solutions
   exercise_5's note that "either arrangement works here."
6. Rewrote `evens` as an appending `for` loop: `[0, 2, 4, 6, 8]`, same as
   the comprehension. Matches Solutions exercise_6.
7. Added a fourth chaining function raising `BadNumber` `from` a freshly
   constructed `ArithmeticError` (not the caught `ValueError`): prints
   the "direct cause" line, same as `explicit()`. Matches Solutions
   exercise_7's prediction and its `ArithmeticError ValueError`
   `__cause__`/`__context__` check.
8. Rewrote the reading half of `context_manager.py` with
   `path.read_text()`: same `one`/`two` output. `read_text()` is
   introduced in the chapter's prose immediately before this listing, so
   the exercise is doable from the chapter alone. Matches Solutions
   exercise_8.
9. Changed `mutating_while_looping.py`'s list to `[2, 2, 1, 3]`: result
   is `[2, 1, 3]` — the first `2` is removed at position 0, the second
   `2` slides into the already-visited slot 0, and the loop moves on to
   position 1. Matches the chapter's "shifting-slots" explanation and
   Solutions exercise_9 exactly, including that the survivor's position
   depends on the data.

All 9 Solutions listings were also run directly from
`build/solutions/04_Foundations--Control_Flow/exercise_[1-9].py`; every
one's stdout matched its `#:` markers verbatim, and Solutions'
prose claims (about `__cause__`/`__context__`, about `read_text()` vs.
`with`, about the survivor's position) all held under inspection of the
executed output.

No suspected-but-unreproduced issues.
