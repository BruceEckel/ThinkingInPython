# Examples that run_examples.py should NOT execute.
#
# Only list examples that cannot run unattended even when fully correct:
# GUI windows, interactive input, infinite loops, or dead external
# dependencies. Do NOT list examples that merely fail today because of
# Python 2 syntax or broken imports. Those are Phase 2 fixes and must stay
# visible as failures until repaired.
#
# One glob pattern per line, matched against the forward-slash relative path
# (e.g. 38_Patterns--Simulation/rats_and_mazes/*
# or 31_Patterns--State_Machines/*gui*.py).
# A "# extract: no-run" line inside a file does the same job inline.
#
# Both trees read this one file: `make run` matches these patterns against
# build/examples/, and `make solutions-run` against build/solutions/. The
# chapter directories are named the same in both, so a pattern specific to
# one tree must name a path that exists only there. Today none of them
# collide.

# Package submodules that need a package context: running one as a script is
# the failure chapter 6 documents (use `python -m`). module4 is the book's,
# module6 the sixth exercise's answer; use_module6.py imports it properly.
06_Foundations--Modules_and_Packages/a_package/module4.py
06_Foundations--Modules_and_Packages/a_package/b_package/module6.py

# Tkinter views (the model + tests run and are checked headlessly).
31_Patterns--State_Machines/tabledriven/vending_view.py
30_Patterns--Observer/box_view.py
38_Patterns--Simulation/chladni_plate/chladni_view.py
